<view class="i-class {{wrapClasses}}">
    <view wx:if="{{overlay}}" class="{{maskClasses}}" bindtap="handleMaskClick"></view>
    <view class="{{containerClasses}}">
        <slot></slot>
    </view>
</view>
