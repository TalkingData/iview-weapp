<view class="i-class {{classes}}">
    <slot></slot>
    <view class="{{dotClasses}}" wx:if="{{ dot }}"></view>
    <view class="{{countClasses}} i-class-alone" wx:elif="{{ count !== 0 }}">{{ finalCount }}</view>
</view>